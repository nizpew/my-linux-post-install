#!/bin/bash
if [ "$#" = '0' ] ; then
echo "Actual free space in the root disk: $(df -h --output=avail / | tail -n 1)"
sleep 2
/home/$USER/exfilemanager/exfilemanager-fzf-implementation
fi






if [ "$1" = '-ai' ] ; then

/home/$USER/exfilemanager/ex-file-manager -ai
fi

